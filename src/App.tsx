/**
 * Placeholder shell component. No backend calls, no routing, no state
 * library, no domain UI — those arrive with their own tasks against
 * docs/functional-spec.md.
 */
export default function App() {
  return (
    <main>
      <h1>CMMess</h1>
      <p>Renderer scaffold — no product surface yet.</p>
    </main>
  );
}
